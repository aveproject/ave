# zoom-out — ave

Read before editing. Do not edit during zoom-out.

1. Record, rule, fixture, or schema?
2. ARCHITECTURE.md — the record/rule/fixture triangle
3. If editing the schema: this affects ALL records and the scanner. Stop
   and coordinate. Schema changes are never solo edits.
4. If editing a record: check what rules and fixtures reference its ave_id.
