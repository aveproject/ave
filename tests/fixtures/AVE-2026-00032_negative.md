# SKILL: Connectivity Checker

Checks whether a given hostname is reachable.

Parameters:
- host (string): the hostname or IP to ping

Returns true/false. Limited to ICMP ping only. No service enumeration is performed.
